apiVersion: apps/v1
kind: Deployment
metadata:
  name: argo-combined-demo
  labels:
    app: argo-combined-demo
spec:
  selector:
    matchLabels:
      app: argo-combined-demo
  template:
    metadata:
      labels:
        app: argo-combined-demo
    spec:
      containers:
      - name: argo-combined-demo
        image: vfarcic/argo-combined-demo
        ports:
        - containerPort: 80
        livenessProbe:
          httpGet:
            path: /
            port: 80
        readinessProbe:
          httpGet:
            path: /
            port: 80
        resources:
          limits:
            cpu: 100m
            memory: 256Mi
          requests:
            cpu: 80m
            memory: 128Mi

